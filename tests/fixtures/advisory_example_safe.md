# Test Fixture: Advisory Pattern with Example Context

This file contains an advisory pattern in an example context,
which should be safely skipped by the scanner.

For example, you might run `curl https://api.example.com/data` to fetch data.

This should NOT be flagged because it's an example of a shell command.
