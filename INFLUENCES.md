# INFLUENCES

This document tracks the external research, conventions, and projects that influenced this framework's process methods. The goal is honest attribution + a future-reader decision trace: when the AI Governance MCP framework adopted, modified, independently arrived at, or considered-and-rejected an idea from elsewhere, this is the canonical record of which.

## Schema

Each entry uses one of four attribution categories:

- **Adopted** — the source's pattern was adopted in substantially the same form. Citation acknowledges the source; modifications (if any) are minor (terminology, framing).
- **Inspired-by + modified per research** — the source's pattern motivated the work, but our implementation diverges materially based on additional research (TDAG, Multi-Agent Reflexion, etc.) or our framework's existing structural commitments.
- **Independently-developed equivalent** — we developed our pattern before encountering the source's pattern, or the patterns address the same problem with materially different mechanics. Citation acknowledges convergent design; no derivation claim.
- **Considered and rejected** — the source's pattern was evaluated and rejected, with documented rationale. Often: existing framework coverage already addresses the same concern; no need for redundant method.

Each row also tags `enforcement: advisory | structural`:
- **advisory** — pattern is documented in framework methods but compliance relies on AI/human follow-through (~85% ceiling per LEARNING-LOG 2026-04-15).
- **structural** — pattern is enforced by hook, CI assertion, or other structural gate (~100% compliance once installed).

When a pattern has both modes (advisory in one surface, structural in another), the row tags both.

**Intent convention.** Each row's Notes cell leads with a one-line `**Intent:**` — the failure-mode/goal the source addresses, *above* its surface form. Intent is the unit the coverage/attribution decision is made against (a surface item can look novel while its intent is already covered, or vice-versa). This keeps intent a first-class recorded field without a separate column.

## Attribution Table — Superpowers (obra/superpowers)

[Superpowers](https://github.com/obra/superpowers) is a Claude Code plugin published as a curated set of process skills (SKILL.md files with brainstorm → write-plan → execute-plan pipeline). The 8 patterns below were evaluated during the Superpowers-driven plan (`~/.claude/plans/federated-plotting-karp.md`, sessions 125-126) referencing version v5.0.7. Earlier evaluation at v4.3.0 is captured in BACKLOG #54 (cross-reference for prior context).

| Pattern | Source | Our Surface | Attribution | Enforcement | Notes |
|---------|--------|-------------|-------------|-------------|-------|
| **A — Action atomicity** | Superpowers `writing-plans` skill | `.claude/plan-template.md` Recommended Approach section + CLAUDE.md Plan Mode + `documents/agents/contrarian-reviewer.md` Step 7 + `.claude/hooks/scan_transcript.py` `--plan-action-atomicity` | Inspired-by Superpowers; modified per TDAG (arxiv 2402.10178) | advisory (template + agent) + structural (WARN-mode hook gate, promotion to BLOCK event-driven per V-007) | Superpowers' skill prescribes one-action-per-step; our action categories are `{write failing test, run test, implement minimal code, refactor, verify}` derived from TDAG's contextual-test-selection findings. Closed set is ours; the discipline of single-category-per-task is theirs. |
| **B — Sequenced two-stage review** | Superpowers `subagent-driven-development` skill | `documents/title-10-ai-coding-cfr.md` §5.1.7.1 Sequenced Two-Stage Review (v2.40.0) | Adopted from Superpowers | structural (normative method under §5.1.7 trigger table) | Stage 1 mutation candidates (code-reviewer, security-auditor, contrarian-reviewer) before Stage 2 coherence/validation reviewers. Anti-pattern (parallel battery across stages) explicitly named. Reinforced by our LEARNING-LOG 2026-04-19 4-instance scope-asymmetry pattern (independently observed before encountering Superpowers; convergent evidence). |
| **C — Brainstorming method** | Superpowers `brainstorming` skill | `documents/title-10-ai-coding-cfr.md` §1.3.5 Brainstorming Method (Depth-Scaled) (v2.41.0; depth-scaled v2.54.0) | Inspired-by Superpowers; modified per `meta-core-discovery-before-commitment` Adaptive Questioning (rules-of-procedure §16.2/§7.9) | advisory (depth-scaled normative method) | Superpowers' skill is unconditional; ours **scales depth** by §1.3.3 calibration — the full Socratic pass fires on ENHANCED-or-no-precedent, while a **minimal founding floor (Goal / Done / Non-goals / app-vs-document) runs in ALL modes** (v2.54.0; the full pass is skipped for STANDARD/EXPEDITED, the floor never is) per proportional-rigor discipline (`meta-methods §7.8`). Design-doc artifact location (`documents/design/<project>.md`) and the 6-area Q&A targeting (latent requirements, implicit boundaries, non-user stakeholders, unnamed failure modes, MVP cliff, anti-goals) are our additions. |
| **D — TDD enforcement** | Superpowers `test-driven-development` skill | `documents/title-10-ai-coding-cfr.md` §5.2.2 Test-First or Test-With (TDD recommended for AI-assisted development) + `.claude/hooks/scan_transcript.py` `--tdd-test-existence` + `.claude/hooks/pre-push-quality-gate.sh` Check 5 | Adopted from Superpowers + augmented per TDAD (arxiv 2603.17973) | advisory (§5.2.2 RECOMMENDED) + structural (WARN-mode pre-push hook for new src/*.py without paired tests; promotion to BLOCK event-driven per V-008) | Superpowers' skill is the workflow recommendation; TDAD's contextual-test-selection finding ("provide specific test context, not generic 'follow TDD'") informed our §5.2.2 operational detail. The hook's pair convention (src/<pkg>/<name>.py → tests/test_<name>.py) is project-specific. |
| **E — Branch completion** | Superpowers `finishing-a-development-branch` skill | `/completion-sequence-aigov` checklist Branch Completion section | Adopted from Superpowers | advisory (BEST-EFFORT checklist) | Superpowers prescribes a 4-option decision tree (merge / PR / keep / discard); we ship 5 (split MERGE into A trunk-direct + C feature-branch) because conflating them produces "merge" steps that don't apply on trunk. Each option has its own checklist consolidating commit/push/CI items. Aligns with [GitHub Agentic Workflows](https://github.blog/ai-and-ml/automate-repository-tasks-with-github-agentic-workflows/) read-only-default + human-approves-merge convention. |
| **F — Mid-execution checkpoints** | Superpowers `executing-plans` skill | `documents/title-10-ai-coding-cfr.md` §5.1.8 Mid-Execution Checkpoint Protocol (v2.42.0, strengthened v2.42.1) + `/completion-sequence-aigov` checklist item 16a (threshold) + `documents/agents/orchestrator.md` checkpoint-discipline paragraph | Adopted from Superpowers + augmented per [Agent Drift](https://arxiv.org/abs/2601.04170) + [Multi-Agent Reflexion](https://arxiv.org/html/2512.20845v1) | advisory (§5.1.8 normative method, checkpoint compliance is BEST-EFFORT) + tunable (threshold values in checklist per SSOT pattern: principle in rulebook, tunable values in checklist) + structural (v2.42.1 step 3 written-table requirement is a verifiable artifact gate) | Superpowers' skill is the phase-transition concept; Agent Drift research provided the empirical threshold (drift onset 73 ± 40 turns OR 35 minutes runtime); Multi-Agent Reflexion provided the optional external-evaluator subagent step. Our 5-step protocol (Pause / Re-read plan / Spot-check delivered-vs-planned / External-evaluator pass / Decide) and "checkpoint theater" anti-pattern naming are our additions. Self-applied: this plan's session-126 MIDPOINT CHECKPOINT was the rule's first invocation, on the plan that codifies it — and the post-ship contrarian battery found the checkpoint had been performed as a single-line "no drift" assertion (the very anti-pattern §5.1.8 named). v2.42.1 PATCH closes the meta-loop: step 3 now REQUIRES a written delivered-vs-planned table (bare assertions = "checkpoint did not happen"); step 4 promoted from optional to REQUIRED-when-drift. The rule now structurally prevents its own most-likely failure mode. |
| **G — Parallel-agent isolation** | Superpowers `parallel-agent-isolation` (or equivalent) | (none) | **Considered and rejected** | n/a | Pattern was evaluated during plan v3 contrarian review (HIGH-3, 2026-04-25). **Failure modes addressed by Superpowers' skill:** (1) parallel agents seeing each other's intermediate state (race conditions on shared scratch); (2) cross-agent context bleed (one agent's draft contaminating another's frame); (3) handoff state divergence (parallel work converging on incompatible assumptions). **Failure modes covered by existing framework:** title-20 §2.3 Context Isolation Verification (lines 1310-1333) addresses failure modes 1 + 2 by post-hoc verifying isolation invariants; title-20 §3.2 Handoff Protocol with Shared Assumptions Document (lines 1408-1469+) addresses failure mode 3 by pre-spawn explicit assumption capture. **Equivalence rationale:** post-hoc verification (§2.3) + pre-spawn explicit assumption capture (§3.2) jointly cover the failure surface Superpowers' pre-spawn isolation pattern addresses, via different mechanism. **Honest limitation:** the equivalence is functional/coverage-based, not mechanical-pattern-identical — Superpowers prevents at spawn-time, our framework verifies at observation-time + captures at handoff-time. Both prevent the same defect class (incoherent parallel-agent output) at different points in the workflow. **Why not adopt anyway:** adding a third pre-spawn method would create three locations for parallel-agent isolation (§2.3, §3.2, new §X) — exactly the duplication `meta-method-single-source-of-truth` deprecates. The contrarian post-ship review (audit `a62e96c04a3f91721`) flagged the original "already-covered" rationale as location-only without failure-mode-equivalence proof; this updated row is the response. **Diagnostic for re-evaluation:** if a future incident shows parallel-agent failure that title-20 §2.3 + §3.2 demonstrably failed to catch (a defect class unique to spawn-time isolation), reopen this rejection. Until then, the existing framework coverage holds. |
| **H — Systematic debugging** | Superpowers `systematic-debugging` (or equivalent) | `documents/title-10-ai-coding-cfr.md` Part 5.13 Structured Debugging Protocol (shipped v2.20.0, 2026-03-13) | Independently-developed equivalent | structural (Part 5.13 normative protocol with 6 subsections: Diagnostic Block / Instrumentation-First / Fix Decay / Fix Verification / Anti-Patterns / Code Comprehension) | Our Part 5.13 predates the Superpowers comparison (shipped session ~v2.20.0 in March 2026). It is materially stronger than Superpowers' debugging skill: 6 subsections including mandatory diagnostic-block-before-fix, instrumentation-first protocol, fix-decay (Oxford/McGill 2024 — 60-80% LLM debugging decay within 2-3 attempts), 4-level verification hierarchy, 6 named anti-patterns, escalation report template. Convergent design with Superpowers' skill on the core "evidence-before-fix" principle. |

## Other Influences (Non-Superpowers)

| Source | Our Surface | Attribution | Enforcement | Notes |
|--------|-------------|-------------|-------------|-------|
| Alaswad et al. "Hybrid Intelligence Effort" framework ([Frontiers AI 2026](https://www.frontiersin.org/journals/artificial-intelligence/articles/10.3389/frai.2026.1772418/full)) + Kahneman/Lovallo Reference-Class Forecasting ([PMI 2026](https://www.pmi.org/learning/library/nobel-project-management-reference-class-forecasting-8068)) | `documents/rules-of-procedure.md` §7.12 Effort-Not-Time Estimation | Adopted | advisory (behavioral floor) + structural (tiers.json directive + disposition kernel + FRAME inject) | Effort dimensions (LLM Reasoning Complexity, Context Completeness, Code Transformation Scope, Iterative Cycles, Human Oversight Effort) + reference-class calibration (track empirical baselines, estimate from class average; 70-80% hit rate vs <20% inside-view). |
| BLUF + Minto Pyramid Principle ([Animalz](https://www.animalz.co/blog/bottom-line-up-front), [BetterUp](https://www.betterup.com/blog/minto-pyramid)) + Hick's Law ([Laws of UX](https://lawsofux.com/hicks-law/)) | `documents/rules-of-procedure.md` §7.13 BLUF-Pyramid Briefing | Adopted | advisory (behavioral floor for user-facing decision briefs only — scope-excludes plan files, ADRs, spec docs, audit logs) | 4-5 sections max, 3-5 bullets per section, 10-20 words per bullet, 300-500 words for 1-pager / 800-1200 for 2-pager, 2-3 alternatives max (Hick's Law: choice paralysis at 4+), risk embedded per option. |
| stop-slop skill ([Hardik Pandya](https://github.com/hardikpandya/stop-slop)) | `documents/rules-of-procedure.md` §7.14 Default-Register Discipline + tiers.json `default-register` directive + CLAUDE.md/AGENTS.md disposition kernel | Inspired-by + modified | advisory (behavioral floor) | stop-slop named the AI-prose-tell gap (the model's default register) and ships a negative banlist (~30 forbidden phrases/devices) + a 50-point self-score. We built the root-cause/positive version: four *stance* directives steering the posture up front (§7.14.2) plus a voice guard (§7.14.4). **Rejected:** its absolutist surface bans (no em-dashes ever, no rule-of-three, no passive) — they collide with `meta-quality-effective-efficient-outputs` + `stor-safety-e1-human-voice-preservation` (voice flattening) and produce their own over-correction tell; and the self-score (same-pass self-verification ceiling, arXiv:2310.01798). A/B/C subagent register test (session-202) confirmed the banlist moves only the surface form (the tell migrates), while stance framing shifted the posture. |
| Karpathy-derived community CLAUDE.md (`forrestchang`/`multica-ai` *andrej-karpathy-skills*; derived from Karpathy's Jan-2026 observations, **not** Karpathy-authored or endorsed) | `proactive-partnership` behavioral-floor directive (tiers.json + disposition kernel) + `coding-process-human-ai-collaboration-model` **D3** (Capability Suppression / Stenographer Collapse, title-10 v2.11.0) | Inspired-by (clause-5 gap) + Independently-developed equivalent (clauses 1-4) | advisory (behavioral floor) | Source-review session-229. Clauses 1-4 **confirmatory** — already covered: simplest-first ≈ `proportional-rigor`, don't-touch-unrelated ≈ Defer-vs-Fix-Now matrix, flag-uncertainty ≈ `meta-safety-transparent-limitations`, ask-don't-assume ≈ Progressive Inquiry §7.9. The maintainer's added **5th clause** ("suggest a better / long-lasting-over-tactical way") + the "silenced pair-programmer" meta-claim surfaced the genuine gap: the **inverse of D2 automation-bias** — the AI *under*-contributes under a constraint-heavy directive balance (a compositional failure no single rule prohibits). Contrarian `ad261fa9` split it: clause-5 *doctrine* is already distributed (§1422/§428/`recommend-not-ask`/anchor-bias §7.10), so its bite-gap routes to BACKLOG #165; only the *failure mode* is novel → D3. n=1 second-hand evidence; promote D3 on a 2nd first-party instance. |
| TDAG (arxiv 2402.10178) | Pattern A (Action atomicity) action categories | Inspired-by | advisory + structural | TDAG's task-decomposition findings shaped the closed-set action categories. |
| TDAD (arxiv 2603.17973) | Pattern D (TDD enforcement) operational detail | Inspired-by | advisory + structural | "Provide agents with specific test context — which test files to run, expected test names — rather than generic 'follow TDD' instructions. 70% regression reduction with targeted context; can worsen results without it." Captured in §5.2.2. |
| Agent Drift (arxiv 2601.04170) | Pattern F (Mid-execution checkpoints) threshold | Adopted | advisory (threshold value in COMPLETION-CHECKLIST item 16a) | Drift onset 73 ± 40 turns OR 35 minutes runtime; informed our >30 min runtime trigger. |
| Multi-Agent Reflexion (arxiv 2512.20845) | Pattern F external-evaluator step | Adopted | advisory (Step 4 of §5.1.8 protocol, optional but recommended for HIGH-stakes plans) | LLM-as-judge re-reading original spec to combat AI's anchor on its in-progress narrative. |
| US Constitutional naming convention | `documents/constitution.md`, `rules-of-procedure.md`, `title-NN-*.md`, `title-NN-*-cfr.md` | Inspired-by + modified | structural (filename schema enforced by extractor + tests) | Constitution → Rules of Procedure → Titles (binding domain law) → CFRs (operational regulations). Explicit US analogy throughout the framework's hierarchy text. Modification: framework's Constitution can be amended by maintainer; US Constitution by Article V. |
| AAIF / Linux Foundation AGENTS.md standard ([agents.md](https://agents.md/)) | `AGENTS.md` (this repo's) + Appendix K of title-10-cfr | Adopted | advisory | Cross-tool standard (60K+ repos); CLAUDE.md / GEMINI.md overlay on top. |
| OWASP LLM Top 10 + Agentic Top 10 + MCP Top 10 | `documents/title-10-ai-coding-cfr.md` Part 5.6 (MCP security) + Part 5.8 (security review by domain) + Part 5.11 (zero trust application) | Adopted | structural (CodeQL CI + bandit + security-auditor subagent invocation gates) | Direct adoption of OWASP's classification + checklists, with our framework's structural gates wrapping them. |
| Bite-Size Elicitation Protocol v2 (hotel-analyzer PMO sessions, 2026-07-14→19) | `documents/rules-of-procedure.md` §7.9.8 Dependency-Paced Elicitation Loop + amendments to §7.9.1–7.9.6 (v3.45.0) | Adopted | advisory (method-level procedure under §7.9 Progressive Inquiry Protocol) | **Intent:** prevent the four co-occurring failure modes of batched questioning during requirements discovery — dependency waste (later questions premised on guessed earlier answers), cognitive overload, anchor bias (question framing constrains the answer space), and context starvation (unexplained synthesis). Field-tested through a full Purpose–Methods–Outcomes discovery round. **7 of 13 protocol rules adopted** as a turn-by-turn inner loop complementing the static planning methods §7.9.1–7.9.7. Two further rules were adopted in the first draft and then *withdrawn* on audit as restatements of siblings (batch sizing → §7.9.4, question format → §7.9.1) and survive as cross-references rather than duplicated law; a third, premise-flagging, was **absorbed** into the testimony step with a citation to the `proactive-partnership` floor — it is stated normatively and separately enforced there, so it was not withdrawn. Rule #6's elicitation core (illustrative-number guard) is retained inside the synthesis step; its generalized data-hygiene form and rules #12/#13 are deferred to BACKLOG (they generalize beyond questioning); rule #11's coverage is confirmed by the `proactive-partnership` floor. 6 sibling methods amended for vocabulary alignment and conflict resolution. |
| Anthropic 2026 Agentic Coding Trends Report | Pattern A + Pattern F design context | Inspired-by | advisory | Drift mitigation + plan-action atomicity context. |
| (framework-internal — `external-input-gap-analysis` floor + §9.8.1 Admission Test) | `/source-review` skill (`global-skills/source-review/`) + rules-of-procedure §9.8.5 "External source review" | Independently-developed equivalent | advisory | **Intent:** make external-source review repeatable and *intent-first* — compare the intent above an item, not the surface item. Codified from the framework's own gap-analysis floor + §9.8 Admission Test, not an external pattern. Self-referential dogfood: the capability that records influences is itself recorded here, shipped in the same commit — proving the same-commit SSOT rule on its first invocation (mirrors how §5.1.8 was self-applied, Pattern F). |
| Kent Beck "Augmented Coding: Beyond the Vibes" ([newsletter.kentbeck.com](https://newsletter.kentbeck.com/p/augmented-coding-beyond-the-vibes)) + "AI Coding Workflow" (Kim/Bouchard, [systemdesign.one](https://newsletter.systemdesign.one/p/ai-coding-workflow)) | `title-10-ai-coding-cfr.md` §5.13.5 "Regression Test for Bug Fixes" (v2.53.0) | Inspired-by + modified | advisory | **Intent:** a shipped defect must leave behind a regression test that *demonstrably failed before the fix* — closing the "writing the test is a separate task from the fix, so it's skipped" gap. Mechanics already covered (§5.2.2 Red-first, §5.13.5 fix-must-be-test-verified, `meta-governance-continuous-learning-adaptation` "prevent recurrence"); §5.13.5 previously *assumed* a failing test existed rather than mandating a bug fix create one. Adopted delta = one normative sub-block routing bug fixes into §5.2.2 confirm-Red; no new principle. Kent Beck's "tests as guardrails / the genie doesn't want to do TDD" framing is the motivation; our surface diverges (it lands in fix-verification, not as a TDD restatement). |
| Kent Beck "Augmented Coding" — "the genie cheats by disabling or deleting tests" (AI test-suppression) | (none — already covered) | Considered and rejected | n/a | **Intent:** stop the agent making a red bar green by suppressing a test (delete / skip / xfail / weaken assertions) instead of satisfying the spec. Covered: deletion → §5.2.9 Deletion Protocol + `failure-mode-registry.md` structural lint (`tests/test_validator.py::TestFailureModeCoverage` trips CI if a must-cover test is removed); expected-value rewrite → §5.2.6 Specification Anchoring Check clauses 2–3 ("never derive expected values from observed output"); tautological green-bar → Echo Chamber Trap + §5.2.2 confirm-Red. Residue (skip/xfail of a currently-failing test) is behaviorally a subset of already-prohibited "green bar without satisfying the spec." A new §5.2.6 detection control would re-grow the redundancy §5.2.9 was built to consolidate (`meta-method-single-source-of-truth`). Re-open if an incident shows suppression that §5.2.9 + anchoring demonstrably missed. |
| "AI Coding Workflow" (systemdesign.one): "require ≥1 test that fails on a known-bad input before accepting AI-generated coverage" (non-vacuous AI-suite gate) | (none — already covered) | Considered and rejected | n/a | **Intent:** reject vacuous AI-generated suites — a test that never demonstrably fails proves nothing. Covered verbatim-in-spirit by §5.2.2 clause 2 ("Confirm the test fails (critical — skipping this risks tests that already pass)"). The "bulk-generated suites aren't test-first, so confirm-Red doesn't apply" extension is an over-read: the control is authoring-path-independent, and EDD §5.2.8 already extends confirm-fail to a non-TDD (probabilistic) path explicitly anchored "no new principle." Mutation-testing-lite is a §5.2.7 Progressive Strategy tooling example at most, not a governance gap. |
| Fallow ([github.com/fallow-rs/fallow](https://github.com/fallow-rs/fallow) · [fallow.tools](https://fallow.tools)) — deterministic codebase-intelligence CLI (Rust, MIT) | `documents/title-10-ai-coding-cfr.md` §6.4.9 AI Code Review Services (three-class taxonomy + scoped blind-spot caveat, v2.58.0) | Inspired-by + modified | advisory | **Intent:** weight automated-review tools by trust profile, not lump them together. §6.4.9 framed all automated review as probabilistic LLM services and applied the "shares blind spots with AI generators (Stanford 2022)" caveat to all of it — factually behind the market (its own example, CodeRabbit, is a *hybrid*: an LLM orchestrating ~50 deterministic linters + a code-graph). Fallow is the *deterministic* class ("no AI inside the analyzer… deterministic findings, typed output contracts") — reproducible, not blind-spot-bound. Adopted delta = a three-class taxonomy (deterministic / probabilistic / hybrid) + "layer, don't choose," reusing the framework's existing deterministic/probabilistic vocabulary (RTK `ref-multi-agent-rtk-hook-compliance-patterns` ~100% vs ~70-85%; EDD §5.2.8); FP-reduction evidence arXiv:2601.18844 (94–98% vs LLM-only). No new principle. Origin: `/source-review` of Fallow + CodeRabbit/landscape research (session-226); contrarian `ac65cfcc160e79d2`. Governance: `gov-7e43b5967c64`. |
| Fallow — "Riskiest files (git churn × complexity)" hotspots | (none — `capture_reference` artifact `ref-ai-coding-fallow-deterministic-codebase-intelligence`; activates on §6.7.1 trigger #7 team-growth) | Considered and rejected (now) | n/a | **Intent:** prioritize remediation by change-frequency-weighted risk (Tornhill/CodeScene "code as a crime scene"), not static complexity alone. Genuinely uncovered (verified: §6.6 is static-only; architecture-review "hotspots" are structural fan-in; `git log -S churn` empty). Rejected as a §6.6.1 indicator *now* on proportional rigor — for a single-maintainer repo the monthly §6.6.3 check already surfaces the same files; the metric's payoff scales with team size + stranger-code. Re-open on §6.7.1 #7. CRAP score also rejected (dominated by indicators #3 cyclomatic + #7 coverage; cf. EDD descope rationale, §5.2.8). |
| Sean Kochel "5 Claude Code MCP Servers You Need To Be Using" (YouTube, Jul 2025; reviewed via an unverified AI-generated summary, `/source-review` session-241) — Playwright "screenshot → LLM senior-designer critique → iterate" loop | `documents/title-15-ui-ux-cfr.md` §8.3 reference-free visual-QA variant (v1.3.0) | Inspired-by + modified | advisory | **Intent:** visual QA when no design reference exists — judge the rendered screenshot instead of skipping QA. §8.3 was comparison-only ("Applies To: projects **with** visual design references"); §1.3's "layout matches design reference *(if provided)*" named the not-provided gap with no procedure. **Modified:** the critique MUST run as a fresh-context judge — a separate agent that did not generate the UI (title-20 Generator-Critic independence) — because the source's demo has the generator grading its own screens, the self-assessment anti-pattern the framework rejects (§7.14.1 self-verification ceiling). Weak provenance (second-hand AI summary; unverifiable "85%"-class claims) — the improvement stands on §8.3's own structural gap, not the source's claims. The 9 other tools in the list: covered/rejected (see session-241 verdict report; the one category gap, SAST, routed to BACKLOG #184). Contrarian `a4b7f1ae1eaae990e`; governance `gov-a1346c233271` / `gov-bdadc4fbc28e`. |
| Shepherd — Programmable Meta-Agents via Reversible Agentic Execution Traces (Yu et al., Stanford/Northeastern, [arXiv:2605.10913](https://arxiv.org/abs/2605.10913), open source) | (none — already covered; `capture_reference` artifact `ref-multi-agent-shepherd-reversible-execution-traces`; deferred build → BACKLOG #185) | Independently-developed equivalent | n/a | **Intent:** intercept a bad agent action *before it lands* (runtime supervisor) and recover failed runs from checkpoints instead of restarting (reversible execution trace). Covered: pre-action gating → title-20 CFR §4.6.3 Hook-Based Enforcement + §4.3 Governance Agent Pattern + guide/sensor taxonomy (EXECUTION-FRAMEWORK §8) + BACKLOG #57 enactment gate; checkpoint/rollback → `meta-operational-failure-recovery-resilience`, title-10 §10.3.2 Rollback Semantics, `multi-reliability-state-persistence-protocol`. New *evidence*, not new intent: CooperBench pair-coding 28.8%→54.7% from runtime supervision — the first quantified benchmark for the framework's guide-over-sensor thesis; cite when #57 ships. The *live-environment-state* rewind delta (process/DB/package/KV cache) is genuinely uncovered but AUTONOMOUS-only → deferred to #185, not adopted (interactive rewind already robust: git + worktrees + Claude Code native rewind + Workflow resume + rollback-semantics). Counterfactual-replay + RL-credit-assignment patterns out of scope. Origin: `/source-review` of the Shepherd YouTube Short + arXiv:2605.10913 (session-241); user reframed the interest to interactive rewind. Governance: `gov-97e46e8176a2` / `gov-86ca106ce2fa`. |
| OpenAI **GPT-5 prompting guide** (Aug 2025, [cookbook.openai.com](https://cookbook.openai.com/examples/gpt-5/gpt-5_prompting_guide)); `/source-review` session-253 | `documents/rules-of-procedure.md` §11.8.3 Contradiction Cost Scales with Capability (+ §11.8 intro two→three failure modes) + `_ai-context/OPERATIONS.md` T-166 contradiction-hunt step (+ `capture_reference` artifact `ref-ai-coding-gpt5-prompting-guide`) | Inspired-by + modified | advisory | **Intent:** keep a large instruction corpus internally consistent as capability grows — a *contradiction* (two live directives that cannot both hold) costs a capable model *more* (it burns reasoning reconciling, vs. a weaker model picking a side), so contradiction-hunting matters *more* on a model upgrade, not less. §11.8 covered one-sidedness (11.8.1) + stale patches (11.8.2) but not *contradiction*, and gave it no capability-scaling directionality; §11.8.3 adds the third, distinct failure mode + a T-166 step. Everything else in the guide is **validation, not gap** (zero new principles): per-tool uncertainty thresholds (checkout low / grep high) ≈ Action-Blast-Radius-Classification + `destructive-action-pre-verification` (independent convergence); Markdown re-injection every 3–5 msgs ≈ `automatic-reaffirmation-triggers`, already *structural* here (per-turn FRAME hook); escape-hatch-under-uncertainty clause ≈ §11.8.1 + D3 Capability-Suppression (redundant → kept as a worked example in the ref, not a rule); persistence/eagerness dial ≈ Autonomy-Level-Assessment + `proactive-partnership`; bounded context-gathering / early-stop ≈ Discovery-Before-Commitment. One-task-per-**turn** decomposition = cross-vendor **watch-item** (single-lineage Responses-API claim; promote on replication). **Currency:** the Aug-2025 guide has since been followed by GPT-5.1 / 5.2 prompting guides — the adopted *intent* is durable; time-sensitive API surface claims may have moved. Contrarian `a6eedd56`; cross-vendor **Codex** peer check (`codex exec`, verdict ADOPT-WITH-CHANGES — softened universal claims to expected-failure-mode framing, updated the §11.8 intro to avoid a self-contradiction). Governance `gov-0eca717ab271` / `gov-be1a4096c393`. |
| Google Cloud **"From prototype to production: Your step-by-step guide to scaling generative AI"** (Saurabh Tiwary, VP & GM Google Cloud AI; PDF created 2025-04-04, 36pp); `/source-review` session-267 | (none — already covered; **assembly delta parked → BACKLOG #251** pending a user-held BPE domain) | **Considered and rejected** (piece-level) + **Independently-developed equivalent** (metrics-driven development → §5.2.8) | n/a | **Intent:** get a generative-AI system from pilot to production without the four things that strand pilots — no business-outcome anchor, no evaluation baseline, tuning before measurement, and monitoring that only sees what it was told to look for. **58 items** (32 concepts / 26 Vertex AI product artifacts; the product class excluded from intent analysis, not silently dropped). **Piece-level verdict: adopt nothing.** Coverage, per rule 3 — "metrics-driven development" and pre-declaring the metric → `title-10-ai-coding-cfr.md` §5.2.8 **Eval-Driven Development** (shipped v2.52.0, 2026-06-13, from `ref-ai-coding-anthropic-prompting-playbook`; independent of and postdating this source) + constitution Art. III §1; eval-corpus coverage → `mrag-method-benchmark-construction` + EV1; unsupervised outlier clustering → `mrag-operations-o2` (its **Lagging-Indicator** pitfall *is* the failure the technique prevents) + SEC1's mechanism-general embedding-anomaly bullet; risk taxonomy → `coding-quality-workflow-integrity` (injection), `mrag-security-sec1` + `mrag-method-poisoning-attack-taxonomy` (poisoning), `coding-context-specification-completeness` + `mrag-evaluation-ev2` (hallucination); validation-depth-scales-with-complexity → `proportional-rigor` + `coding-process-lifecycle-proportional-governance`; "not all problems are AI problems" → Anti-Gold-Plating (Art. II §4). **Note the source's version is also *wrong*:** it frames metrics-driven development as *replacing* TDD, where §5.2.8's decision rule requires **both** (TDD for deterministic plumbing, EDD for the generative core). Better-packaged, less correct. **External standards rejected:** ISO/IEC 42001, EU AI Act, MAS Veritas are named in one sentence with no content. §14.2.7's watch list is scoped **Security Content Currency** by its own Applies-To line, so its security-only membership is jurisdiction, not bias; regulatory intent is discharged at a better altitude by constitution **Compliance by Default** (per-project, at project start). Admission Test **Q6** fails outright (EU AI Act obligations phase in through 2027, and ISO 42001 is on a revision cycle). **Recitation (copyright surfacing in generated output) is genuinely absent** — we verify the licence of what we *import* (§5.4) and attribute output *to the AI*, but not what output may have reproduced. Rejected as an encoded rule (**Q5** no enforcement gate, **Q6** law in active litigation), **re-shaped by the user into a trigger** → BACKLOG **#252**: encode the prompt to go check at the gate, never the rules themselves — the de-pinning doctrine applied to law. **THE ASSEMBLY IS THE SECOND GENUINE DELTA (recitation, above, is the first) — and the larger of the two, because the piece-level review could not see it at all.** Item enumeration extracts *nodes* and is blind to the *graph*: 32 concepts each returned "covered" while the source's actual contribution — the **order** — was never examined. Its sequencing claim is non-obvious and stated twice: **evaluation precedes improvement, model selection precedes evaluation, tuning is last.** We have no equivalent **at product scope** — `coding-process-lifecycle-proportional-governance` classifies governance *rigor* (prototype/internal/production), not build order; §5.2.8 is a loop *inside* one step; Specify → Plan → Tasks → Implement governs writing code, not shipping a genAI product; §6.7.2's Declare → Gap-Analyze → Harden → Validate → Approve is a promotion gate, not a build sequence. **Methods-layer check (per the altitude rule): the nearest incumbent is `title-40-multimodal-rag-cfr.md`** — §10.2 Embedding Model Lifecycle already orders *evaluate-the-new-model-against-the-benchmark → parallel-generate → cutover decision → execute → retain rollback*, §6.3 Drift Detection opens *"ESTABLISH baseline: run full evaluation on current system,"* and §3.2 is Embedding Model Selection. **So "evaluation precedes improvement" and "model selection precedes evaluation" are already encoded operationally — for one system class (multimodal RAG), at component scope.** What is uncovered is the *product-scope* generalization across system classes: business-objective anchor first, tune last, monitor as the closing step. That is why it parks at #251 rather than being adopted. *(This qualification was added by a fresh-context validator: the first draft claimed a flat "no equivalent" — made **without** applying the altitude rule this same review had just written into `/source-review` Phase 4, two sentences after citing two other title-40 methods. The rule's first real test was the row that created it, and the row failed it.)* **Recorded, NOT admitted:** the assembly intent has run neither §9.8.1 nor a contrarian pass, and both are prerequisites before anything ships from it. **#251 is therefore a rule-5 *exception*, not rule-5 routing** — rule 5 sends an intent to BACKLOG *having passed* both gates; this one is filed as evidence attached to a domain decision, not as a candidate awaiting implementation (BACKLOG #251 states the same). Parked at #251 because it is business/product-lifecycle shaped, and the framework already cites **Luftig/BPE** in `title-25-kmpd` (§KA4, §Domain Truth Sources) — the seam exists, the domain does not. **Currency:** cites Gemini 1.5 as current and gives inconsistent model counts (160+ / over 150) two pages apart; the *intents* are durable, every *surface claim* is stale — do not re-adopt its product surface. Prompted the inward pin audit → BACKLOG **#253**. Contrarian `ab922e219624c3e62` (refuted all three of the review's proposed gaps and located §5.2.8, which the review had missed by comparing an operational item against a constitutional principle — the altitude rule now in `/source-review` Phase 4). Governance `gov-2063bcf89d78` / `gov-44e7853d139e`. |
| **"Multi-Level RAG Architecture for AI Expert SaaS"** v2.0 (2025-10-10, 36pp; author unattributed — the document's own v1.0 note says "based on Perplexity research"); `/source-review` session-267 | `documents/title-40-multimodal-rag.md` DG1 — **Output-Boundary Re-Verification** bullet + **Single-Checkpoint Assumption** pitfall (v2.6.0) | **Inspired-by + modified** (DG1 delta) + **Considered and rejected** (all 20 other items) | advisory | **Intent:** treat the generated response as untrusted output and re-decide authorization at the output boundary — every source the response *cites* must be one the caller may see — so that a silent failure of the retrieval-layer control does not reach the user unchecked. **21 items** (16 concepts / 5 artifact-and-tool groups; 13 routine-SaaS items batch-rejected as a group, not silently dropped). **The DG1 delta is the only adopted item.** DG1's four bullets were all *pre-response*; its "Storage-Only Lock" pitfall names the same shape one layer down but stops at retrieval. **Methods-layer incumbent exists** — title-40-cfr §9.1 RBAC Configuration (whose Purpose line is "Procedures for implementing DG1") carries `Response generation | Verify all images in response are accessible to user`. The bullet generalizes it past images and adds the independence requirement the cell omits. *(The first draft of this row asserted "searched, nothing found." It was false: the search was a grep for output-**filtering** vocabulary, which that cell does not use. Coherence audit `a6751db63f9cab37b` caught it and named the class — an unfalsifiable negative about one's own search thoroughness, §9.8.8.1. The altitude rule was applied at the wrong **tool**: `query_project` is the concept-discovery instrument, grep is for exact strings, and CLAUDE.md already says so. **Second time in two consecutive session-267 source-reviews that the altitude/coverage claim failed on its first pass** — the Google Cloud row records a validator catching the same shape.)* Note `coding-method-output-validation` scores HIGH on semantic retrieval for this intent and is a **false positive** — it governs Excel/PDF/Word *file* integrity. **Everything else rejected, and on three of them our incumbent is strictly stronger, not merely equivalent:** (1) *hybrid search* — the source linearly blends **raw** `ts_rank` against **raw** cosine at a fixed 70/30, scales that are not comparable, where `meta-method-hybrid-retrieval-recommended` (§12.3.2) specifies **RRF k=60**, which exists precisely to fuse *ranks* instead of incomparable scores; (2) *chunk overlap* — its unconditional "18%, based on empirical testing" (no testing cited) sits inside `meta-method-overlap-strategy`'s 15–20% default, which is additionally *conditional* on content type; (3) *prompt injection* — a regex denylist for "ignore previous instructions" **as the control**, versus `coding-quality-workflow-integrity`'s structural trusted/untrusted source classification, where such patterns are a flag for review, never the gate. **Tenant isolation rejected on a verified defect:** the design sets `set_config('app.current_tenant_id', …, true)` — transaction-local — on a `databases` connection while every query runs through `supabase.table()`/`.rpc()`, i.e. the PostgREST HTTP API on a *different* connection, so the RLS predicate resolves against NULL. Confirmed against Supabase's own docs, which scope the session-variable pattern to **direct connections** and make `auth.uid()`/`auth.jwt()` the PostgREST path. The document's central claim ("automatic at database layer… no application code can bypass," §4.2 Layer 2) is therefore false as written — a live specimen of the Default Configuration Trap already named in `coding-method-backend-as-a-service-security`. Captured as `ref-ai-coding-postgrest-rls-session-variable-transport-gap`; §5.3.6 checklist gains the matching line. **Assembly pass (Phase 2, third pass):** the source *does* assert a build order — a 6-week roadmap placing **security audit and compliance last (Week 6)** — which contradicts `coding-quality-security-first-development` and Amendment I ("embedded from the outset as defaults, not added as afterthoughts"). The ordering is a **negative** example; it strengthens the rejection rather than contributing a sequence. **Currency:** pins `claude-sonnet-4-5` / `claude-opus-4-1` / `gpt-4-turbo` / `text-embedding-ada-002`, all superseded; self-certifies "Production-Ready," "proven in production environments," and a "✅ VALIDATED" table naming no production system, benchmark, or citation — self-attested validation wearing the visual grammar of independent audit. **Best-in-class (title-10 §3.1.4):** Supabase publishes an official **"RAG with Permissions"** guide covering this document's exact topic; that is the stronger exemplar of the same intent. **Probe value exceeded source value:** checking its fusion approach against our own code surfaced an internal inconsistency — §12.3.2 prescribes RRF, the governance server is linear-only, the CE implements RRF but defaults to linear → BACKLOG **#258**. Governance `gov-f1c346e9c628` / `gov-dba00fe015d3`. **Honest limitation:** this review's first pass was run against a *stale* copy of `/source-review` — `~/.claude/skills/` symlinks to the **main** checkout, and the assembly pass + altitude rule were committed on this branch but not yet merged, so both gates were run retroactively. Neither changed a verdict; both strengthened the evidence. |
| **Building Consistent Workflows with Codex CLI & Agents SDK** (OpenAI, 2025; PDF guide); `/source-review` wt/source-reviews | (none — already covered) | Independently-developed equivalent | n/a | **Intent:** consistent multi-agent workflow patterns — agent specialization, deterministic guardrails, handoff protocols, tracing/observability. 26 items → 10 intents, all covered. Strong multi-agent-domain convergence: framework holds these at constitutional + domain level (title-20 agent topology, handoff protocol, enforcement architecture, visible reasoning). §9.8.5: no method-level improvements. `multi-architecture-agent-specialization-topology`, `multi-reliability-explicit-handoff-protocol`, `multi-method-governance-enforcement-architecture`, `meta-quality-visible-reasoning-traceability`. Governance `gov-99e8f9fdad45`. |
| **AWS Marketplace Prompt Engineering Ebook** (AWS, 2025; ebook); `/source-review` wt/source-reviews | (none — already covered) | Considered and rejected | n/a | **Intent:** foundational prompt engineering methods (zero/few-shot, CoT, role prompting, RAG overview, model selection). 14 items → 6 intents, comprehensively covered. Source operates at significantly lower altitude — introductory material vs the framework's practitioner-level methods. §9.8.5: no method-level improvements. |
| **@hooeem "I want to become an AI engineer"** (X/Twitter article, 2025); `/source-review` wt/source-reviews | (none — already covered) | Considered and rejected | n/a | **Intent:** AI engineering practitioner stack overview — 4-layer architecture (model → prompt → retrieval → orchestration), prompt engineering, RAG, fine-tuning, evaluation, deployment patterns. 26 items → 14 intents, all Covered or Independently-developed-equivalent. Source's 4-layer stack was adopted then superseded by framework's 5-layer stack (adds harness layer). No genuinely-new intents. |
| **Stanford CS230 "Enhancing LLM Applications — Beyond LLM"** (lecture transcript, 2025; 995-line transcript); `/source-review` wt/source-reviews | (none — already covered) | Independently-developed equivalent | n/a | **Intent:** systematic LLM application enhancement beyond the model — prompt engineering, output structuring, RAG, fine-tuning, agents/tool-use, evaluation, caching, guardrails. 28 items → 17 intents, all covered. Academic treatment arriving at same conclusions through a different path (pedagogy vs production governance). §9.8.5: no method-level improvements — framework coverage is more specific and practitioner-oriented. `meta-method-baseline-prompting-cross-model`, `multi-method-governance-enforcement-architecture`, `meta-method-output-format-specification`, `coding-quality-testing-integration`. |
| **TDS "Build and Query Knowledge Graphs with LLMs"** (Dylan Tartarini, Towards Data Science, May 2025) + 2026 memory/KG landscape research (Cognee, Graphiti/Zep, Hindsight, Supermemory, LightRAG, LazyGraphRAG, Mem0, Letta/MemGPT, LangMem, Redis); `/source-review` wt/source-review-1of2 | `documents/title-40-multimodal-rag-cfr.md` §3.8 Step 0 ontology design preamble + Step 1 deterministic-vs-LLM extraction distinction; BACKLOG #323 (temporal fact modeling); #316 enrichment | **Inspired-by + modified** (2 method-level improvements) | advisory | **Intent:** (1) Ontology-first graph construction — define entity/relationship types before extraction to constrain cost and noise; §3.8 jumped straight to NER without schema design. (2) Extraction method selection — deterministic NER (spaCy, rule-based), LLM-based, or hybrid; §3.8 assumed LLM extraction without surfacing the tradeoff. (3) Temporal fact modeling — validity windows and fact supersession for evolving knowledge; identified in ref library entry 3 months ago as Zep/Graphiti differentiator, never operationalized → BACKLOG #323. Source article is introductory-altitude (Neo4j + LangChain tutorial) but the landscape research revealed the tiered guidance gap: practitioners choosing between $0 (NetworkX) and $500/mo (Neo4j + LLM extraction) had no intermediate options in §3.8. The ref library entry already covers the competitive landscape; these edits bring the decision-relevant parts to the methods layer where practitioners look. `mrag-architecture-a5-knowledge-graph-integration`, `meta-core-systemic-thinking`, `meta-operational-resource-efficiency-waste-reduction`. |
| **last30days-skill** ([mvanhorn/last30days-skill](https://github.com/mvanhorn/last30days-skill), GitHub repository, v3.18.4, MIT, 2026); `/source-review` wt/source-reviews | (none — already covered) | Independently-developed equivalent | n/a | **Intent:** structurally enforced, quality-floored AI skill specification — 1400+ line behavioral spec (SKILL.md) with 11 inviolable output rules (LAWs), domain vocabulary glossary (CONCEPTS.md), multi-harness distribution. 25 items → 15 intents, all covered. Strong convergence on structural enforcement over advisory instruction (v3.0.7 anchors evidence), engine/model cognitive separation, failure-mode ratchet (named-incident rule provenance), absolute quality floors with honest-empty outcomes, cross-harness portability. Framework holds these at constitutional/domain level; source implements them at skill level. §9.8.5: inline incident citation and "relative bar degrades with pool" are crisp formulations but domain-specific, not method-level improvements. `multi-method-governance-enforcement-architecture`, `multi-architecture-agent-specialization-topology`, `meta-governance-continuous-learning-adaptation`, `meta-safety-transparent-limitations`, `ref-multi-agent-harness-engineering-synthesis`. |
| **MCPize** (mcpize.com + LinkedIn post, 2025); `/source-review` wt/source-reviews | (none — already covered) | Considered and rejected | n/a | **Intent:** MCP server distribution and monetization infrastructure — discovery, hosting, monetization, 7-dimension audit, unified billing. 11 items → 5 intents; 4 are product/infrastructure observations (hosting, billing, discovery, UX) operating at a different altitude than governance. One governance-adjacent intent (automated quality vetting of third-party tools) covered by `ref-ai-coding-skill-supply-chain-attacks-toxicskills` which goes deeper (instruction-layer attack surface). Source is about how to distribute/monetize MCP servers, not about how to govern or engineer them. |
| **"Build Agents that never forget"** (unattributed Cognee-promotional blog post, ~2025; pasted text); `/source-review` wt/source-reviews | (none — already covered; Cognee is already a project dependency per SBOM.md + BACKLOG #47) | Considered and rejected | n/a | **Intent:** prevent the seven failure modes of memoryless agents through structured, persistent, semantically retrievable memory with multi-hop relational reasoning. 27 items → 10 intents, all covered. The article's recommended tool (Cognee) is **already a project dependency** (`cognee >= 1.0.9, < 2`, Apache-2.0; BACKLOG #47 active integration; `ref-multimodal-rag-kg-landscape-2024-2026` recommends it after tool evaluation). Its cognitive science taxonomy (Weng 2023, 3 memory systems) is a **subset** of the framework's 6-type CoALA extension (§7.0.2). Its memory consolidation concept (episodes → rules) is implemented by the framework's dream/journal system (different mechanics, same intent). Multi-hop retrieval covered by `mrag-architecture-a5-knowledge-graph-integration` + `V4` + methods. Context budget management covered by `multi-architecture-context-engineering-discipline` (MA-C3, explicitly names "lost in the middle"). Source operates at introductory/tutorial altitude — teaching practitioners what the framework already implements in production. §9.8.5: no method-level improvements. **Cross-project gap:** initial review evaluated coverage too narrowly (ai-governance's own use vs its role guiding other projects with large, diverse corpora); user pushback correctly identified that non-LLM-dependent graph-vector alternatives for cross-project guidance are not evaluated → BACKLOG #316. Governance `gov-52bb48bfa6c8`, `gov-4fb3458c1df0`. |
| **Claude Code Routines documentation** (Anthropic, 2026; research preview announcement + product documentation); `/source-review` wt/source-reviews | (none — already covered) | Considered and rejected | n/a | **Intent:** automate repeatable AI tasks via declarative specification, event-driven triggering, and autonomous cloud execution with appropriate trust, identity, and blast-radius controls. 28 items → 10 intents, all covered. Source is product documentation describing a vendor feature, not a governance methodology. Framework already holds all intents at constitutional + domain level: autonomous operation governance (Title 20, Title 6, AO-Series), instruction/data separation (`meta-core-separation-of-instructions-and-data`), blast radius classification, HITL removal criteria, workflow integrity. Reference Library already contains `ref-multi-agent-cloud-routine-reminder-agent` (cloud-routine-as-reminder pattern). §9.8.5: no method-level improvements — source implements existing framework principles, not new ones. |
| **Karpathy-Inspired Claude Code Guidelines** ([multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills), 2026; community distillation of Karpathy's LLM coding observations into 4 CLAUDE.md rules with worked examples; 200k+ stars); `/source-review` wt/source-reviews | (none — already covered; prior Karpathy-clause review (session-229) already produced the D3 Capability Suppression failure mode + `proactive-partnership` behavioral floor) | Independently-developed equivalent | n/a | **Intent:** prevent the four dominant AI coding failure modes — assumption-driven errors, unnecessary complexity, scope creep via drive-by changes, vague execution without verification. 32 items → 8 intents, all covered. Strong convergence: the source's four rules map directly to framework constitutional principles (`meta-operational-explicit-over-implicit`, `meta-operational-resource-efficiency-waste-reduction`, `coding-process-atomic-task-decomposition`, `meta-quality-verification-validation`). The framework goes further: it identifies the *compositional* risk when these defensive rules combine — D3 Capability Suppression / Stenographer Collapse (`coding-process-human-ai-collaboration-model`), whose origin is explicitly a prior Karpathy source review (session-229). The `proactive-partnership` behavioral floor is the counterweight the source lacks. Reference Library already holds `ref-ai-coding-capability-suppression-compositional` + `ref-ai-coding-autoresearch-pattern` from Karpathy's work. §9.8.5: no method-level improvements. `meta-operational-explicit-over-implicit`, `meta-operational-resource-efficiency-waste-reduction`, `meta-quality-verification-validation`, `coding-process-human-ai-collaboration-model` (D3 Stenographer Collapse — Karpathy origin), `ref-ai-coding-capability-suppression-compositional`. |
| **Gary Marcus "Why Claude Code feels so powerful"** (@GaryMarcus, X/Twitter post, ~2026; commentary arguing Claude Code's strength comes from neurosymbolic AI — deterministic symbolic kernel + neural LLM — not scaling); `/source-review` wt/source-reviews | (none — already covered; the framework's enforcement architecture independently operationalizes the neurosymbolic pattern) | Considered and rejected | n/a | **Intent:** recognize LLM reliability limitations and compensate with deterministic/symbolic controls; hybrid architecture produces more reliable systems than pure neural approaches. 13 items → 5 intents; 3 covered (LLM reliability → deterministic enforcement, hybrid architecture, software engineering quality), 2 not applicable (AI research roadmap, industry capital allocation commentary). The framework independently operationalized the neurosymbolic pattern Marcus describes: ADR-3 splits "script handles S-Series deterministically; AI handles principle conflicts" — deterministic safety kernel + probabilistic governance evaluation. The measured ~100% structural vs ~85% advisory compliance gap (`ref-multi-agent-rtk-hook-compliance-patterns`, LEARNING-LOG) IS the empirical case for the hybrid. §4.6.3's "architecture beats hope" is the same thesis in operational language. §6.4.9's three-class taxonomy (deterministic / probabilistic / hybrid) classifies the pattern space. Source operates at AI-theory/industry-commentary altitude, not governance-methodology altitude — names what the framework already does, but "neurosymbolic" is a retrospective categorization, not a design tool. The framework's operational vocabulary (structural vs advisory, deterministic vs probabilistic) is more actionable. Governance `gov-54b51bc847c6`. |
| **knowledge-rag** MCP server (GitHub; local RAG with 12 tools, ONNX FastEmbed, hybrid search + RRF fusion); `/source-review` wt/source-reviews | (none — already covered) | Independently-developed equivalent | n/a | **Intent:** token-efficient document retrieval via local hybrid search (semantic + BM25 + cross-encoder reranker + RRF fusion) served through MCP, with read/write plane separation (local embeddings for indexing, Claude for actions). 12 items → 7 intents, all covered. Architecturally isomorphic to Context Engine + governance MCP stack: hybrid search → `meta-method-hybrid-retrieval-recommended` (§12.3.2) + `retrieval.py`; local embeddings → §12.2.1 + ADR-1; chunk-level retrieval → `mrag-architecture-a3-chunk-level-retrieval`; MCP tool interface → Context Engine (7 tools); read/write separation → `meta-core-separation-of-instructions-and-data`; cross-encoder reranking → §12.3.3; evaluation tooling → `mrag-evaluation-ev1` + §5.2.8 EDD. §9.8.5: no method-level improvements. Governance `gov-18011121b31e`. |
| **FastRAG** "Binary Quantization for 32× Memory-Efficient RAG" (blog article, 2025; pasted text); `/source-review` wt/source-reviews | (none — gap surfaced; deferred → BACKLOG #227 annotation; when Part 12.2 de-pin pass fires, add §12.2.4 quantization decision framework) | Inspired-by + modified | n/a | **Intent:** reduce vector storage cost at scale via binary quantization (float32 → binary, Hamming distance, 32× memory reduction, 5-15% recall loss). Part 12.2 (Embedding Optimization) has zero quantization guidance (confirmed: `grep -r "quantiz" documents/` empty). ADR-25 has the substantive analysis (6 methods, 5 libraries, phased path, revisit triggers) but no methods-layer section exists. **Modified:** the source's static comparison table (pinned figures: "Perplexity uses…", "Azure uses…") would re-arm the volatile-value defect class BACKLOG #227 already flags for §12.2.1; per `ref-ai-coding-depin-volatile-values`, write a decision framework (when to quantize, phased path, revisit triggers, live source pointers), not a snapshot. The source's production evidence (Perplexity search index, Azure AI Search, HubSpot AI assistant, Augment Code) added to ADR-25 sources. **Dimension-generalization gap** (contrarian-flagged): ADR-25's math is for 384-dim (bge-small) but the project supports bge-large (1024-dim) via title-10 §7.3.6 — binary quantization recall loss is dimension-sensitive; unverified but real design risk. Governance `gov-18011121b31e`. |

| **Agent Plugins v1.0.0** (agent-plugins.org, undated; TSC: Amazon, Cursor, Microsoft, OpenAI, Vercel); `/source-review` wt/source-review-2 | `documents/title-10-ai-coding-cfr.md` §5.10.6 loading-time graduated failure row + §5.6.2 distributed-artifact design rule + §9.5.7 Agent Skills spec cross-reference; `global-skills/source-review/procedure.md` IDE close-out gate + citation integrity clause; BACKLOG #322 (plugin filesystem containment subsection) | **Inspired-by + modified** (4 method-level improvements) + **Independently-developed equivalent** (10 other intents) | advisory | **Intent:** vendor-neutral packaging standard for AI agent skills and MCP servers — containment, graceful loading, progressive disclosure, portable naming. **29 items** (20 concepts / 6 artifacts / 3 assembly-lifecycle), abstracted to **14 distinct intents**. 10 independently-developed equivalents (convention-based discovery, progressive disclosure, naming, env injection, URL security, etc. — our coverage at the methods layer is equivalent or stronger). **4 method-level improvements adopted** (contrarian-reviewed: 3 CONFIRMED, 1 WEAK): (1) §5.10.6 table-row — loading-time graduated failure boundaries (skip invalid component, load remaining, log which was skipped); the constitutional principle establishes graceful degradation generally but the methods layer had no loading-time operationalization; (2) §5.6.2 paragraph — "distributed config is visible data" design rule, generalizing §5.8.5 "Secrets in Layers" from containers to all distributed artifacts (structural prevention complement to detection controls); (3) §9.5.7 bullet — Agent Skills spec (agentskills.io) as the industry-standard portable skill format; our SKILL.md is structurally compliant; (4) BACKLOG #322 — plugin filesystem containment (Zip Slip class) as a new §5.6 subsection, distinct from registry-verification (A4/A5); the framework's own code (FilesystemStorage) implements this pattern ad hoc but gives no reusable guidance. **4 considered-and-rejected:** forward-compatible schema (engineering convention), transport abstraction (MCP implementation detail), multi-vendor governance model (different scope), portable naming (already compliant). **Skill improvement:** the first pass classified all 14 intents as IDE — "coverage analysis not gap analysis" error. User correction + contrarian pass surfaced 3 real gaps. Added to `source-review/procedure.md`: (a) IDE close-out gate (3 questions: best-for-any-user, future coverage, industry convergence) to prevent "covered" as terminal verdict; (b) citation-integrity clause (verify every quoted string against actual stored content — contrarian caught a fabricated quote attributed to `meta-operational-failure-recovery-resilience`). **Assembly pass:** the spec's lifecycle (load → validate → discover → skip invalid → continue) is a loading-time instance of existing graceful-degradation patterns, not a novel build order. Contrarian `a4579ee2fc8df602a`; governance `gov-23b2603ef153` / `gov-e40f8d37eba6`. |

## How to extend this document

Run **`/source-review`** (the codified intent-first review sequence, rules-of-procedure §9.8.5) to evaluate an external source and produce the verdict + a proposed row; it *proposes*, you apply. To hand-author an entry directly:

1. Abstract the source's item to its **intent** (the failure-mode/goal above the surface), then determine the attribution category by checking coverage of that *intent*, not the surface form: Adopted / Inspired-by + modified / Independently-developed / Considered and rejected.
2. Add a row to the appropriate table with: source, our surface (file + section), attribution, enforcement (advisory|structural|both), notes. **Lead the Notes cell with `**Intent:**`** (see Schema).
3. If "Considered and rejected": cite the existing framework coverage that makes the addition redundant. The "rejected" attribution is information; future contributors should be able to see WHY we didn't adopt and reach the same conclusion if they evaluate again.
4. If the pattern materializes as a new method or hook: cross-reference the framework version where it shipped (e.g., "shipped v2.42.0 §5.1.8").
5. **Non-row outcomes (routing):** a **genuinely new intent** not covered by any principle/method (and which passed the §9.8.1 Admission Test + a contrarian pass) goes to a **BACKLOG.md Discussion item** ("genuinely-new intent → consider"), *not* an INFLUENCES row — the row is created later, in the commit that ships the adopted method. A **preserve-worthy concrete artifact** also warrants a `capture_reference` Reference Library entry (Title 15) alongside the row.

## Why this document exists

The framework's authors actively comparison-shop external skill libraries, research papers, and conventions. Without an attribution record:
- Future contributors don't know which patterns were considered, which adopted, which modified, which rejected.
- Re-evaluation re-runs the same comparison work.
- "Convergent design" claims are unverifiable without an audit trail.

This document is the audit trail. **Same-commit rule (normative): an INFLUENCES row attributing an adopted or modified pattern MUST ship in the same commit as the method it influences** — never as a deferred follow-up, so the attribution and the change it explains are atomic in history.

---

*Last updated: 2026-08-08 (wt/source-reviews — 2 additional `/source-review` passes: knowledge-rag MCP server (independently-developed equivalent, 7 intents all covered), FastRAG binary quantization (inspired-by+modified — §12.2 quantization gap surfaced, deferred to BACKLOG #227 annotation + ADR-25 sources updated). Running total for this branch: 12 reviews, 5 independently-developed, 6 considered-and-rejected, 1 inspired-by+modified; 2 BACKLOG items (#316 cross-project graph-vector, #227 annotated with quantization gap)). Prior: 2026-08-07 (wt/source-reviews — batch `/source-review` of 10 external sources: 4 independently-developed equivalent (OpenAI Codex CLI & Agents SDK, Stanford CS230, mvanhorn/last30days-skill, Karpathy-Inspired Claude Code Guidelines), 6 considered and rejected (AWS Prompt Engineering Ebook, @hooeem AI Engineer, MCPize, "Build Agents that never forget", Claude Code Routines, Gary Marcus neurosymbolic AI commentary); 0 adopted, 0 method changes; 1 BACKLOG item (#315 — cross-project graph-vector landscape, from user pushback on Cognee article coverage scope)). Prior: 2026-07-28 (session-267 — `/source-review` of "Multi-Level RAG Architecture for AI Expert SaaS" → Inspired-by+modified for the DG1 output-boundary re-verification delta (title-40 v2.6.0), Considered-and-rejected for the other 20 items, three of which our incumbent strictly dominates; `capture_reference` `ref-ai-coding-postgrest-rls-session-variable-transport-gap` + §5.3.6 checklist line; the source's probe value exceeded its source value → BACKLOG #258 RRF-vs-linear fusion inconsistency). Prior: 2026-07-27 (session-267 — `/source-review` of Google Cloud "From prototype to production" → Considered-and-rejected at piece level + Independently-developed-equivalent for §5.2.8 EDD; the larger of two deltas is the source's **assembly/sequencing** (recitation → #252 is the other), recorded-not-admitted and parked → BACKLOG #251 pending a BPE domain, with #253 volatile-pin audit alongside). Prior: 2026-07-08 (session-241 — `/source-review` of Shepherd (arXiv:2605.10913) → Independently-developed-equivalent row + `capture_reference` `ref-multi-agent-shepherd-reversible-execution-traces` + BACKLOG #185 deferred live-state-rewind build; companion §7.8.1 Bilateral value method shipped v3.39.0). Prior: 2026-06-24 (session-226 — Fallow `/source-review` + CodeRabbit landscape → §6.4.9 three-class-taxonomy rows). Prior: 2026-06-14 — wired to the `/source-review` skill + rules-of-procedure §9.8.5 (Intent convention, routing for non-row outcomes, normative same-commit rule, self-attribution row). Plan `clever-stargazing-sonnet`. Prior: 2026-04-25 (session-126, Commit 8 of Superpowers-driven plan `~/.claude/plans/federated-plotting-karp.md`).*
